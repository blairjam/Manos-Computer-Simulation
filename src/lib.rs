#![allow(dead_code)]
pub mod registers;
pub mod memory;

/*#[cfg(test)]
mod test {
    use mcs::*;

    fn add_one_two() {
        #[test]
        let a = 1;
        let b = 2;
         assert_eq!(a + b, add(a, b));
    }
}*/
